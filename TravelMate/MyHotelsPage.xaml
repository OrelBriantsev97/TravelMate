<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:TravelMate.Controls"
             x:Class="TravelMate.MyHotelsPage"
             NavigationPage.HasNavigationBar="False"
             BackgroundColor="#C1ECF5">

    <Grid RowDefinitions="*, Auto">
        <!-- Main Content -->
        <ScrollView Grid.Row="0">
            <!-- Wrapped StackLayout inside ScrollView -->
            <StackLayout Padding="20" Margin="0,50,0,0">
                <Label Text="My Hotels" FontSize="36" FontFamily ="Chi" FontAttributes="Bold" 
                       TextColor="#32545c" HorizontalOptions="Center" Margin="0,0,0,10" />

                <CollectionView x:Name="HotelsCollectionView">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Frame BackgroundColor="White" CornerRadius="10" Margin="5" Padding="10" HasShadow="True">
                                <StackLayout>
                                    <Image Source="{Binding LogoUrl}" WidthRequest="330" HeightRequest="70" Aspect="AspectFill"/>
                                    <Label Text="{Binding HotelName}" FontAttributes="Bold" FontFamily="Chi" 
                                           TextColor="#3b6069" FontSize="18"/>
                                    <Label Text="{Binding Address}" TextColor="Gray" FontSize="14"/>
                                    <Label Text="{Binding CheckInDate, StringFormat='Check-in: {0:yyyy-MM-dd}'}" 
                                           TextColor="Gray" FontSize="14"/>
                                    <Label Text="{Binding CheckOutDate, StringFormat='Check-out: {0:yyyy-MM-dd}'}" 
                                           TextColor="Gray" FontSize="14"/>
                                </StackLayout>
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <Label Text="➕ Add New Hotel" TextColor="Black" FontSize="18" FontFamily="Chi"
                       FontAttributes="Bold" HorizontalOptions="Center" Margin="0,10,0,20">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Tapped="addHotel"/>
                    </Label.GestureRecognizers>
                </Label>
            </StackLayout>
        </ScrollView>

        <ContentView x:Name="NavigationContainer" Grid.Row="1"/>
    </Grid>

</ContentPage>
