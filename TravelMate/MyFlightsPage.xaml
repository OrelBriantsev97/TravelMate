<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TravelMate.MyFlightsPage"
             NavigationPage.HasNavigationBar="False"
             BackgroundColor="#C1ECF5">

    <Grid RowDefinitions="Auto,*">
        <!-- Page Title -->
        <Label Text="My Flights" FontSize="24" FontAttributes="Bold" TextColor="#3b6069"
               HorizontalOptions="Center" Margin="0,20,0,10"/>

        <!-- Flights List -->
        <CollectionView x:Name="FlightsListView" ItemsSource="{Binding Flights}" Grid.Row="1"
                        SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame BackgroundColor="White" CornerRadius="10" Margin="10" Padding="15" HasShadow="True">
                        <StackLayout>
                            <Label Text="{Binding FlightNumber}" FontAttributes="Bold" TextColor="#3b6069"/>
                            <Label Text="{Binding Airline}" TextColor="Gray"/>
                            <Label Text="{Binding DepartureCity}" TextColor="Black"/>
                            <Label Text="{Binding ArrivalCity}" TextColor="Black"/>
                            <Label Text="{Binding DepartureTime}" TextColor="Gray"/>
                            <Label Text="{Binding ArrivalTime}" TextColor="Gray"/>
                        </StackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Bottom Navigation Bar -->
        <Grid Grid.Row="2" BackgroundColor="White" HeightRequest="70">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- My Hotels -->
            <StackLayout Grid.Column="0" VerticalOptions="Center" HorizontalOptions="Center">
                <Image Source="hotel_icon.png" HeightRequest="30" WidthRequest="30">
                    <Image.GestureRecognizers>
                        <TapGestureRecognizer Tapped="ShowHotels"/>
                    </Image.GestureRecognizers>
                </Image>
                <Label Text="My Hotels" FontSize="12" TextColor="Black"/>
            </StackLayout>

            <!-- My Flights -->
            <StackLayout Grid.Column="1" VerticalOptions="Center" HorizontalOptions="Center">
                <Image Source="flight_icon.png" HeightRequest="30" WidthRequest="30">
                    <Image.GestureRecognizers>
                        <TapGestureRecognizer Tapped="ShowFlights"/>
                    </Image.GestureRecognizers>
                </Image>
                <Label Text="My Flights" FontSize="12" TextColor="Black"/>
            </StackLayout>


            <!-- Home -->
            <StackLayout Grid.Column="2" VerticalOptions="Center" HorizontalOptions="Center">
                <Image Source="home_icon.png" HeightRequest="35" WidthRequest="35">
                    <Image.GestureRecognizers>
                        <TapGestureRecognizer Tapped="ShowHome"/>
                    </Image.GestureRecognizers>
                </Image>
                <Label Text="Home" FontSize="12" TextColor="Black"/>
            </StackLayout>

            <!-- Placeholder (Future Feature) -->
            <StackLayout Grid.Column="3" VerticalOptions="Center" HorizontalOptions="Center">
                <Image Source="question_icon.png" HeightRequest="30" WidthRequest="30"/>
                <Label Text="Coming Soon" FontSize="12" TextColor="Gray"/>
            </StackLayout>

            <!-- Profile -->
            <StackLayout Grid.Column="4" VerticalOptions="Center" HorizontalOptions="Center">
                <Image Source="profile_icon.png" HeightRequest="30" WidthRequest="30">
                    <Image.GestureRecognizers>
                        <TapGestureRecognizer Tapped="ShowProfileOptions"/>
                    </Image.GestureRecognizers>
                </Image>
                <Label Text="Profile" FontSize="12" TextColor="Black"/>
            </StackLayout>
        </Grid>

    </Grid>

</ContentPage>
